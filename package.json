{
  "name": "spacenerds",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Web-only arcade roguelike in the Asteroids lineage. Vector graphics, synth audio, Newtonian zero-g physics, permadeath.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "littlejsengine": "^1.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
